# The API URL for the backend.
# Leave this blank or undefined during local development to use the Vite proxy (which points to localhost:3000).
# For production, set this to your actual deployed backend URL.
VITE_API_URL=https://api.example.com
